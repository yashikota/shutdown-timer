name: Release

on:
  push:
    tags: [ 'v*' ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    name: Build & Publish
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        rid: [ win-x64, win-arm64 ]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get version from tag
        id: version
        shell: pwsh
        run: |
          if ($env:GITHUB_REF -match '^refs/tags/v(.+)$') {
            $version = $matches[1]
          } else {
            $version = "dev"
          }
          echo "VERSION=$version" >> $env:GITHUB_OUTPUT
          echo "Version: $version"

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Setup winapp CLI
        uses: microsoft/setup-WinAppCli@v0.1

      - name: Restore
        run: dotnet restore

      - name: Publish
        run: >
          dotnet publish
          shutdown-timer.csproj
          -c Release
          -r ${{ matrix.rid }}
          -f net8.0-windows10.0.20348.0
          --self-contained true
          /p:WindowsAppSDKSelfContained=true
          /p:PublishSingleFile=false
          /p:IncludeNativeLibrariesForSelfExtract=false
          -o publish/${{ matrix.rid }}

      - name: Generate certificate
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path cert | Out-Null
          winapp cert generate --output cert\cert.pfx

      - name: Sign executables
        shell: pwsh
        run: |
          $pubDir = "publish\${{ matrix.rid }}"
          Get-ChildItem -Path $pubDir -Filter "*.exe" | ForEach-Object {
            winapp sign $_.FullName cert\cert.pfx --password password
          }

      - name: Create MSIX package
        shell: pwsh
        run: |
          $Version = "${{ steps.version.outputs.VERSION }}"
          $Rid = "${{ matrix.rid }}"
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          winapp pack "publish\$Rid" --output "dist\shutdown-timer-$Version-$Rid.msix" --cert cert\cert.pfx --cert-password password --self-contained

      - name: Collect & Zip
        shell: pwsh
        run: |
          $Rid = "${{ matrix.rid }}"
          $Version = "${{ steps.version.outputs.VERSION }}"
          $pubDir = "publish\$Rid"
          if (-not (Test-Path $pubDir)) { throw "Publish directory not found: $pubDir" }

          New-Item -ItemType Directory -Force -Path dist | Out-Null
          $dist = "dist\shutdown-timer-$Version-$Rid"
          if (Test-Path $dist) { Remove-Item $dist -Recurse -Force }
          New-Item -ItemType Directory -Force -Path $dist | Out-Null

          Copy-Item -Path (Join-Path $pubDir '*') -Destination $dist -Recurse -Force

          $zip = "dist\shutdown-timer-$Version-$Rid.zip"
          if (Test-Path $zip) { Remove-Item $zip -Force }
          Compress-Archive -Path (Join-Path $dist '*') -DestinationPath $zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: shutdown-timer-${{ steps.version.outputs.VERSION }}-${{ matrix.rid }}
          path: dist/shutdown-timer-${{ steps.version.outputs.VERSION }}-${{ matrix.rid }}.zip
          if-no-files-found: error

      - name: Upload MSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: shutdown-timer-${{ steps.version.outputs.VERSION }}-${{ matrix.rid }}-msix
          path: dist/shutdown-timer-${{ steps.version.outputs.VERSION }}-${{ matrix.rid }}.msix
          if-no-files-found: error

      - name: Upload certificate
        if: matrix.rid == 'win-x64'
        uses: actions/upload-artifact@v4
        with:
          name: certificate
          path: cert\cert.cer
          if-no-files-found: error

  release:
    name: Create GitHub Release (on tag)
    if: startsWith(github.ref, 'refs/tags/')
    needs: publish
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
